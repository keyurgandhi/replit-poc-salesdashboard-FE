# No environment variables needed for the frontend.
# API calls are proxied to http://localhost:8080 by the Vite dev server.
# For production builds, set VITE_API_BASE_URL if your API is on a different origin.
